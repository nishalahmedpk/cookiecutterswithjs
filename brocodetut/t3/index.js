// if else similar to C
// .checked returns boolean for checkboxes and radio
// tarnary ops same as C
// switch case same as C

// String Manipulation
    // var.charAt(index);
    // .indexof("x") -> index
    // .lenght
    // .trim() -> white space X
    // .toUpperCase
    // .toLowerCase
    // .repeat(n)
    // .startsWith(" ")
    // .includes(" ")
    // .replaceAll("a","b")
    // .padstart(15,0)
    // .padend()

    // .slice(star,end->exclusive)
    // let mail = "nishal@gmail.com";
    // let user = mail.slice(0,mail.indexOf("@"));
    // let dom = mail.slice(mail.indexOf("@")+1);
    // console.log(`usernam ${user} and domain is ${dom}`);

// === & !== compares datatype also

// loops like C

// Arrays
// let x = like python
// accessing also like python
// array.push(elem)
// .pop
// .unshift

// Spread operator ...array/strings;

// Rest opposite of spread /Packing

// Foreach and Map

// .forEach(); splits array and calls the function for every element; changes values

// array.ap(function) - similar to foreach but returns new array; original array there

// .filer(funct) -> removes elements which return false;

// .reduce(callback) somewhat like a continous function somwhere        
// .toFixed(decimalplaces);

// function expressions
// const hello = function() {}

// setTimeout(function,timeinmilliseconds)

// const hello = () => console.log("hello");

// Object  contains properties(variables) and methods
// this - basically self, access variable inside the object.
// const c1 = {
//  stuff;
// }

// Construcor
// function Car(parameters) {
//     this.name = name;
//     ...
// }
// const c1 = new Car(parameters);

// Classes basically python class
// class Nishal{
//     constructor(args){
//         this.args=args;
//     } 

// }

// Static = stuffs in class basiclly like const

// Inheritane => child extends parents

// super, to access parent elemnt
// if we want a constructer in child
// we need to create parent before child

// getters and setters set variable(var){
//  return this.private=var}

